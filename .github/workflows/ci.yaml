---
name: ci-pr-fast

'on':
  pull_request:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**/*.md'

concurrency:
  group: ci-pr-fast-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Check versions
        run: |
          flutter --version
          dart --version
          DART_VERSION=$(dart --version 2>&1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          if [ "$(printf '%s\n' "3.6.0" "$DART_VERSION" | sort -V | head -n1)" != "3.6.0" ]; then
            echo "Dart $DART_VERSION is below required 3.6.0"
            exit 1
          fi

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            .dart_tool/build
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: ${{ runner.os }}-pub-

      - name: Bootstrap Flutter local.properties
        run: bash scripts/setup_flutter_localprops.sh

      - name: Install dependencies
        run: flutter pub get

      - name: Flutter analyze
        run: flutter analyze

  tests:
    runs-on: ubuntu-latest
    needs: analyze
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Check versions
        run: |
          flutter --version
          dart --version
          DART_VERSION=$(dart --version 2>&1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          if [ "$(printf '%s\n' "3.6.0" "$DART_VERSION" | sort -V | head -n1)" != "3.6.0" ]; then
            echo "Dart $DART_VERSION is below required 3.6.0"
            exit 1
          fi

      - name: Cache Pub dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            .dart_tool/build
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: ${{ runner.os }}-pub-

      - name: Bootstrap Flutter local.properties
        run: bash scripts/setup_flutter_localprops.sh

      - name: Install dependencies
        run: flutter pub get

      - name: Run Dart tests (labeled PRs only)
        if: ${{ contains(github.event.pull_request.labels.*.name, 'run-tests') }}
        run: |
          DART_TESTS=$(find test -type f -name "*_test.dart" -exec grep -L "package:flutter" {} +)
          if [ -n "$DART_TESTS" ]; then
            dart test --reporter expanded $DART_TESTS
          else
            echo "No pure Dart tests found."
          fi

      - name: Run Flutter tests (labeled PRs only)
        if: ${{ contains(github.event.pull_request.labels.*.name, 'run-tests') }}
        run: |
          FLUTTER_TESTS=$(find test -type f -name "*_test.dart" -exec grep -l "package:flutter" {} +)
          if [ -n "$FLUTTER_TESTS" ]; then
            flutter test --machine $FLUTTER_TESTS
          else
            echo "No widget tests found."
          fi

      - name: Skip tests (default)
        if: ${{ !contains(github.event.pull_request.labels.*.name, 'run-tests') }}
        run: echo "PR tests are skipped. Add label 'run-tests' to enable."
